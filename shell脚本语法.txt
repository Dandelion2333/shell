基础知识：
	1、获取当前路径：
		SHELL_FOLDER=$(cd "$(dirname "$0")";pwd)
	2、打印：
		普通打印：
			echo xxx
		打印变量：注意，变量都有{}括起来会好些。
			echo ${SHELL_FOLDER}
	3、字符拼接：直接放一起就可以了
		c=$a$b	
		c=$a"xyz"$b
		c=$a$b.txt
		
			
我对shell的理解：
	1、它的用途是把指令都写一块，然后一次性执行了。
	2、shell脚本的语法与平时写的操作指令差不多。